name: feedback_ui
description: "Flutter Web UI for Leo Feedback MCP"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  web_socket_channel: ^3.0.2
  flutter_markdown: ^0.7.6+2
  provider: ^6.1.4
  shared_preferences: ^2.5.3
  web: ^1.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
